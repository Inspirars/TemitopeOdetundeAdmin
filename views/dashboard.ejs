<%- include("./layout/header.ejs") %>
<link rel="stylesheet" href="/stylesheets/style.css">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/jquery-modal/0.9.1/jquery.modal.min.css" />
<title><%=title %></title>
</head>
<body>
<nav class="flex bg-grey-100 justify-between items-center py-4 px-4 md:px-10 md:pt-8 lg:px-20 font-inter fixed w-full overflow-hidden " id="nav">
    <div class="lg:flex items-center gap-[72px]">
        <a href="/"><img src="/images/logo.svg" alt="Temitope svg"></a>
    </div>
    <div class="lg:flex text-white">
        <a href="/logout">
            <button class="text-sm md:text-lg p-2 md:px-5 md:py-4 rounded font-semibold bg-red-400 border-2 border-red-400 hover:bg-grey-100 hover:border-red-400 hover:text-red-500 transition-all duration-700">
                Log Out
            </button>
        </a>
    </div>
</nav>
  <main class="px-2 md:px-5 lg:px-20 pt-24 pb-11 md:pt-32 lg:pt-44 lg:py-[clamp(85px,15.62vh,174px)]">
    <h1 class="text-2xl md:text-4xl">
        Welcome <%=user%>
    </h1>
    <h3 class="my-4 ">
        Manage Post
    </h3>
    <table>
        <thead>
            <tr>
                <th class="lg:w-[10%]">S/N</th>
                <th >Title</th>
                <th colspan="1">Author</th>
                <th>Action</th>
              </tr>
        </thead>
        <tbody>
            <%if(postsTitle){%>
                <%postsTitle.forEach((detail,idx)=> {%>
                    <div id=<%=`ex${idx}`%> class="modal delete mb-12">
                        <p class="text-center mb-7 font-bold">Are You Sure You Want To See This Go</p>
                        <div class="text-center ">
                            <a href=<%=detail.delete%> >
                                <button class="text-sm md:text-lg p-2 md:px-3 md:py-2 rounded text-white font-semibold bg-red-300 border-2 border-red-300 hover:bg-grey-100 hover:text-red-500 transition-all duration-700">
                                    Delete
                                </button>

                            </a>
                        </div>
                    </div>
                    <tr>
                        <td><%=idx + 1 %></td>
                        <td><%=detail.title%></td>
                        <td>Temitope Odetunde</td>
                        <td>
                            <a href=<%=detail.edit%>>
                                <button class="text-sm md:text-lg p-2 md:px-3 md:py-2 rounded text-black-200 font-semibold bg-grey-100 border border-grey-500 hover:bg-white  transition-all duration-700">
                                    Edit
                                </button>
                            </a>
                            
                            <a href=<%=`#ex${idx}`%>  rel="modal:open" class="text-sm md:text-lg p-2 md:px-3 md:py-2 rounded text-white font-semibold bg-red-300 border-2 border-red-300 hover:bg-grey-100 hover:text-red-500 transition-all duration-700">
                                    Delete
                            </a>
                        </td>
                    </tr>
                <%})%>
           <%}else{%>
            <p>No post yet</p>
           <%}%>
        </tbody>
      </table>

      <a href="/create">
        <button class="my-3 text-sm md:text-lg p-2 md:px-5 md:py-4 rounded font-semibold bg-purple text-white border border-purple hover:bg-grey-100 hover:text-purple transition-all duration-700">
            Create New Post
        </button>
      </a>
      <%if(postsTitle){%>
        <%postsTitle.forEach((post,idx)=>{%>
            

              
        <%})%>
      <%}%>
      
  </main>

<%- include("./layout/footer.ejs") %>

